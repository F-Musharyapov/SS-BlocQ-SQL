//Task 44
//Найдите названия всех кораблей в базе данных, начинающихся с буквы R.

SELECT class_name
FROM (
    SELECT name AS class_name
    FROM ships
    UNION
    SELECT ship AS class_name
    FROM outcomes) t1
WHERE class_name LIKE 'R%'